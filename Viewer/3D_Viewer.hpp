/**
 * @file 3D_Viewer.hpp
 * @author diabloan & tandyfor
 * @brief The main project documentation file for generating a doxygen report
 * @date 2024-09
 *
 * @copyright Copyright (c) 2024
 */

/**
 * @mainpage 3D Viewer 2
 *
 * <b>the program will allow you to open .obj files and work with displaying
 * objects</b>
 *
 * The program was developed in C++ language standard C17 using gcc compiler.
 * The project uses additional libraries and modules QT.
 *
 * The program is developed in accordance with the principles of OOP.
 *
 * Google Style was used when writing the code.
 *
 * Main targets for working with the project:
 * - install:
 *   - To install a project, test it with tests, and view documentation, use the
 * <b>all</b> target
 *   @code {.c}
 *   make all
 *   @endcode
 *
 *   - To install a project without building tests, use the <b>install</b>
 * target
 *   @code {.c}
 *   make install
 *   @endcode
 * - uninstall:
 *   - To remove a program, use the <b>uninstall</b> target
 *   @code {.c}
 *   make uninstall
 *   @endcode
 *
 * - tests:
 *   @endcode
 *   - To test a project with tests, use the <b>test</b> target
 *   @code {.c}
 *   make test
 *   @endcode
 *
 * This program was developed as part of the <b>School 21</b> educational
 * project
 *
 * @author diabloan & tandyfor
 */

/**
 * @brief Main targets Makefile
 *
 * @page Makefile
 * - <b>all</b> - installs the program, tests it with tests and displays project
 * documentation
 * - <b>install</b> - installs the program in the specified directory
 * - <b>uninstall</b> - removes the project from the previously specified
 * directory
 * - <b>clean</b> - deletes temporary files
 * - <b>dvi</b> - generates and opens project documentation
 * - <b>dist</b> - creates a project archive
 * - <b>tests</b> - tests project functions
 * - <b>gcov_report</b> - generates a report on code coverage by tests
 */